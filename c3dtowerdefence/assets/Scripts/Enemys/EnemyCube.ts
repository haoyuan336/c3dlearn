import { _decorator, Component, Node, CCInteger, v3, Vec3, tween, path, Tween, CameraComponent, Vec2, v2, JsonAsset, game ,isValid} from 'cc';
import { State } from './../util/State'
import { EnemyBase } from './EnemyBase';
const { ccclass, property } = _decorator;
@ccclass('EnemCube')
export class EnemyCube extends EnemyBase {
    // init(gameConfig: JsonAsset, pathNodeList: Node[]){
       
    //     super.init(gameConfig, pathNodeList);
    // }
}
